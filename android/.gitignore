# Upload / signing material — never commit
*.keystore
*.jks
keystore.properties
android.keystore

# Bubblewrap / Gradle output
app/
build/
.gradle/
local.properties
app-release*.apk
app-release*.aab
manifest-checksum.txt

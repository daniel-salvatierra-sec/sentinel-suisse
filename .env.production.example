# Production template — copy to `.env` in repo root before:
#   docker compose -f docker-compose.prod.yml up -d --build
#
# Never commit the filled `.env`.

APP_ENV=production
SECRET_KEY=
PII_ENCRYPTION_KEY=

# Public HTTPS URL (must match DOMAIN below)
PUBLIC_APP_URL=https://linkswiss.ch
DOMAIN=linkswiss.ch
TRUSTED_HOSTS=linkswiss.ch,www.linkswiss.ch

# Public UI features (off by default in production)
PUBLIC_SEARCH_ENABLED=true
PUBLIC_SIGNUP_ENABLED=true
SIGNUP_AUTO_VERIFY=false

# Database password (used by postgres service + DATABASE_URL override in compose)
POSTGRES_PASSWORD=

# Admin — generate hash: python scripts/generate_admin_hash.py
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=

RATE_LIMIT=60/minute

# WhatsApp webhook (Meta → https://linkswiss.ch/api/v1/webhooks/whatsapp)
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=
WHATSAPP_INBOUND_AUTO_VERIFY=true
WHATSAPP_VERIFY_KEYWORD=OK

# Email
NOTIFIER_MODE=smtp
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Live ingest — keep false until legal review
INGEST_DISPATCH_ALERTS=false
LISTING_FRESH_HOURS=48
INGEST_HOMEGATE_LIVE=false
# Set true on VPS after docs/providers/jobs.md checklist — Geneva job feed via jobs.ch
INGEST_JOBS_LIVE=false
INGEST_FLATFOX_LIVE=false
FLATFOX_NORTH=46.32
FLATFOX_SOUTH=46.12
FLATFOX_EAST=6.30
FLATFOX_WEST=5.95
FLATFOX_REGIONS=geneva,zurich,bern,basel,lausanne,lugano,luzern,stgallen,sion,fribourg,neuchatel,winterthur,nyon,vevey,montreux,thun,chur,yverdon,chauxdefonds,biel,zug,schaffhausen,uster,aarau,morges,bulle,martigny,sierre,monthey,delemont,olten,baden,wil,frauenfeld,solothurn,langenthal,interlaken,liestal,kreuzlingen,locarno,mendrisio,chiasso,bellinzona,brig,schwyz,emmen,dietikon,horgen,annemasse,ferney,stjulien,gaillard,thonon,annecy,loerrach,weil,konstanz,waldshut,como,varese,domodossola
FLATFOX_MAX_PER_REGION=50
FLATFOX_MAX_LISTINGS=2500
FLATFOX_REQUEST_PAUSE_SECONDS=0.35
INGEST_IMMOSCOUT_LIVE=false
INGEST_NEWHOME_LIVE=false
INGEST_ANIBIS_LIVE=false
# Set true on VPS after docs/providers/jobup.md checklist — Geneva job feed via jobup.ch
INGEST_JOBUP_LIVE=false
# JobCloud crawl depth (pages per city) and logistics role keywords (see config.py defaults)
JOBCLOUD_MAX_PAGES=3
INGEST_LEBONCOIN_LIVE=false
INGEST_INDEED_FR_LIVE=false

# France Travail (ex-Pole Emploi) — official OAuth2 API, not scraping. Free account
# + app at https://francetravail.io, subscribe to "Offres d'emploi v2" to get these.
FRANCE_TRAVAIL_CLIENT_ID=
FRANCE_TRAVAIL_CLIENT_SECRET= # gitleaks:allow
FRANCE_TRAVAIL_DEPARTEMENT=74
# Comma keywords for Haute-Savoie border jobs (logistics, warehouse, transport)
FRANCE_TRAVAIL_KEYWORDS=magasinier,logistique,chauffeur,manutention,cariste,approvisionnement
FRANCE_TRAVAIL_MAX_PAGES=4
INGEST_FRANCE_TRAVAIL_LIVE=false

# Adzuna — official self-serve job aggregator API, built for redistribution, not
# scraping. Free key at https://developer.adzuna.com/signup (instant, no approval wait).
ADZUNA_APP_ID=
ADZUNA_APP_KEY= # gitleaks:allow
ADZUNA_COUNTRY=ch
ADZUNA_KEYWORDS=
ADZUNA_LOCATION=Geneve
INGEST_ADZUNA_LIVE=false

# Adzuna France — same keys, Haute-Savoie / Geneva border.
INGEST_ADZUNA_FR_LIVE=false
ADZUNA_FR_LOCATION=Haute-Savoie
ADZUNA_FR_KEYWORDS=

# SmartRecruiters — official, keyless Postings API used by several large Geneva
# employers for their public career sites (e.g. HUG, SGS). No signup, no API key,
# not scraping (see docs/providers/smartrecruiters.md).
SMARTRECRUITERS_COMPANIES=HUG,SGS
SMARTRECRUITERS_FETCH_DETAILS=true
INGEST_SMARTRECRUITERS_LIVE=false

# Richemont — public Workday CXS API (same JSON API careers.richemont.com's own JS
# calls). No signup, no API key, not scraping (see docs/providers/richemont.md).
RICHEMONT_EXTRA_LOCATION_HINTS=
INGEST_RICHEMONT_LIVE=false

# Lombard Odier — same public Workday CXS API as Richemont
# (see docs/providers/lombard-odier.md). No signup, no API key, not scraping.
LOMBARD_ODIER_EXTRA_LOCATION_HINTS=
INGEST_LOMBARD_ODIER_LIVE=false

# Logitech — same public Workday CXS API as Richemont
# (see docs/providers/logitech.md). No signup, no API key, not scraping.
LOGITECH_EXTRA_LOCATION_HINTS=
INGEST_LOGITECH_LIVE=false

# Procter & Gamble — same public Workday CXS API as Richemont
# (see docs/providers/procter-gamble.md). No signup, no API key, not scraping.
PROCTER_GAMBLE_EXTRA_LOCATION_HINTS=
INGEST_PROCTER_GAMBLE_LIVE=false

# Temenos — same public Workday CXS API as Richemont
# (see docs/providers/temenos.md). No signup, no API key, not scraping.
TEMENOS_EXTRA_LOCATION_HINTS=
INGEST_TEMENOS_LIVE=false

# STMicroelectronics — public Eightfold JSON API
# (see docs/providers/stmicroelectronics.md). No signup, no API key.
INGEST_STMICROELECTRONICS_LIVE=false

# Phase B — Stripe Premium (CHF). Webhook URL:
#   https://linkswiss.ch/api/v1/webhooks/stripe
# Events: checkout.session.completed, customer.subscription.deleted
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=
STRIPE_ENABLE_TWINT=true
STRIPE_ALLOW_PROMOTION_CODES=true
# Coupon: 50% off for 3 months (repeating) + promotion code LINKSWISS50
STRIPE_LAUNCH_PROMO_CODE=LINKSWISS50
STRIPE_LAUNCH_PROMO_PERCENT=50
STRIPE_LAUNCH_PROMO_MONTHS=3
# One-time listing boost (Checkout mode=payment)
STRIPE_FEATURE_PRICE_ID=
STRIPE_FEATURE_DAYS=7
STRIPE_SPONSOR_PRICE_ID=
STRIPE_SPONSOR_DAYS=30
# Optional multi-app operator hub (JSON array). Leave empty = LinkSwiss only.
# OPS_APPS_JSON=[{"id":"other","name":"Other App","public_url":"https://example.com","admin_path":"/admin","status":"planned"}]

# Optional error tracking
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.05

# Optional AI assistant (free-form chat in the guide bot). Leave ASSISTANT_API_KEY
# empty to keep the assistant disabled (frontend falls back to the button-only guide).
# Costs are pay-per-use, kept in check by ASSISTANT_RATE_LIMIT and
# ASSISTANT_MAX_OUTPUT_TOKENS. Works with any OpenAI-compatible endpoint:
#
# Option A — OpenAI (https://platform.openai.com/api-keys):
# ASSISTANT_API_KEY=sk-...
# ASSISTANT_API_BASE_URL=https://api.openai.com/v1/chat/completions
# ASSISTANT_MODEL=gpt-4o-mini
#
# Option B — Google Gemini, OpenAI-compatible mode (https://aistudio.google.com/apikey).
# Gemini 3 models "think" before answering and count that reasoning against
# ASSISTANT_MAX_OUTPUT_TOKENS, so set ASSISTANT_REASONING_EFFORT=minimal to avoid
# truncated replies. Leave ASSISTANT_REASONING_EFFORT empty for OpenAI (gpt-4o-mini
# rejects this parameter).
# ASSISTANT_API_KEY=AIza...
# ASSISTANT_API_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/chat/completions
# ASSISTANT_MODEL=gemini-3.6-flash
# ASSISTANT_REASONING_EFFORT=minimal
ASSISTANT_API_KEY=
ASSISTANT_API_BASE_URL=https://api.openai.com/v1/chat/completions
ASSISTANT_MODEL=gpt-4o-mini
ASSISTANT_REASONING_EFFORT=
ASSISTANT_MAX_OUTPUT_TOKENS=700
ASSISTANT_MAX_INPUT_CHARS=500
ASSISTANT_MAX_HISTORY_MESSAGES=6
ASSISTANT_RATE_LIMIT=20/day

# Play Store TWA (package ch.linkswiss.app). SHA-256 of the upload / Play App
# Signing certs, comma-separated. Leave empty until the first Play upload.
ANDROID_PACKAGE_ID=ch.linkswiss.app
PLAY_ASSETLINKS_SHA256=
